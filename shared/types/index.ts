/**
 * Cross-service shared TypeScript types live here (e.g., User, ScanResult,
 * shared API enums) to keep frontend and backend from drifting on shape
 * definitions, per the Implementation Blueprint's recommendations.
 *
 * Intentionally empty in Milestone 0 - the first real shared type is
 * introduced in Milestone 1 alongside the User model.
 */
export {};
